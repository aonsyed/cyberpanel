<script setup lang="ts">
import { PhCheckCircle as CheckCircle, PhInfo as Info, PhWarning as Warning, PhX as X, PhXCircle as XCircle } from "@phosphor-icons/vue";
import { sessionStore } from "../store";
const icons={healthy:CheckCircle,info:Info,warning:Warning,critical:XCircle};
</script>
<template><div class="notice-stack" aria-live="polite"><article v-for="notice in sessionStore.state.notices" :key="notice.id" class="notice" :class="`notice-${notice.tone}`"><component :is="icons[notice.tone]" :size="20" weight="fill"/><div><strong>{{notice.title}}</strong><p>{{notice.body}}</p></div><button type="button" :aria-label="`Dismiss ${notice.title}`" @click="sessionStore.dismiss(notice.id)"><X :size="15"/></button></article></div></template>
<style scoped>.notice-stack{position:fixed;z-index:100;right:20px;bottom:20px;width:min(390px,calc(100vw - 32px));display:grid;gap:9px}.notice{display:grid;grid-template-columns:auto 1fr auto;gap:11px;align-items:start;padding:14px;background:var(--surface);border:1px solid var(--border-strong);border-left:3px solid var(--info);border-radius:var(--radius);box-shadow:var(--shadow)}.notice-healthy{border-left-color:var(--healthy)}.notice-warning{border-left-color:var(--warning)}.notice-critical{border-left-color:var(--critical)}.notice>svg{color:var(--info)}.notice-healthy>svg{color:var(--healthy)}.notice-warning>svg{color:var(--warning)}.notice-critical>svg{color:var(--critical)}strong{display:block;font-size:13px}p{margin:4px 0 0;color:var(--muted);font-size:12px;line-height:1.45}.notice button{border:0;background:transparent;color:var(--subtle);cursor:pointer;padding:1px}</style>
