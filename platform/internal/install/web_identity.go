package install

// Provisioned by the web authority installer hook before native validation.
// Workers must not inherit either panel authority or the vendor admin account.
const IdentityWebWorker IdentityID = "cyberpanel-web"
