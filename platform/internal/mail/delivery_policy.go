package mail

import (
	"context"
	"database/sql"
	"errors"
	"fmt"
	"net/netip"
	"strings"
	"time"
)

type DeliveryDecision string
const ( DeliveryPermit DeliveryDecision="permit"; DeliveryDefer DeliveryDecision="defer"; DeliveryReject DeliveryDecision="reject" )
type DeliveryLimit struct { HourlyMessages uint64 `json:"hourly_messages"`; MonthlyMessages uint64 `json:"monthly_messages"`; HourlyRecipients uint64 `json:"hourly_recipients"`; MonthlyRecipients uint64 `json:"monthly_recipients"`; MaxMessageBytes uint64 `json:"max_message_bytes"`; MaxRecipientsPerMessage uint32 `json:"max_recipients_per_message"` }
type DeliveryRequest struct { ID string `json:"id"`; TenantID string `json:"tenant_id"`; DomainID DomainID `json:"domain_id"`; MailboxID MailboxID `json:"mailbox_id"`; Authenticated bool `json:"authenticated"`; ClientIP string `json:"client_ip"`; EnvelopeFrom Address `json:"envelope_from"`; Recipients []Address `json:"recipients"`; MessageBytes uint64 `json:"message_bytes"`; At time.Time `json:"at"` }
type PolicyDecision struct { RequestID string `json:"request_id"`; Decision DeliveryDecision `json:"decision"`; Code string `json:"code"`; Detail string `json:"detail"`; HourMessages uint64 `json:"hour_messages"`; MonthMessages uint64 `json:"month_messages"`; ReservedAt time.Time `json:"reserved_at"` }
type DeliveryPolicyStore struct { DB *sql.DB; ResolveLimit func(context.Context,string,DomainID,MailboxID)(DeliveryLimit,error) }
const deliveryPolicySchema=`CREATE TABLE IF NOT EXISTS mail_delivery_reservations_v2(request_id TEXT PRIMARY KEY,tenant_id TEXT NOT NULL,domain_id TEXT NOT NULL,mailbox_id TEXT NOT NULL,hour_bucket TEXT NOT NULL,month_bucket TEXT NOT NULL,messages INTEGER NOT NULL,recipients INTEGER NOT NULL,bytes INTEGER NOT NULL,decision TEXT NOT NULL,decision_code TEXT NOT NULL,created_at TIMESTAMP NOT NULL);CREATE INDEX IF NOT EXISTS mail_delivery_hour ON mail_delivery_reservations_v2(tenant_id,mailbox_id,hour_bucket);CREATE INDEX IF NOT EXISTS mail_delivery_month ON mail_delivery_reservations_v2(tenant_id,mailbox_id,month_bucket);`
func (s DeliveryPolicyStore)Bootstrap(ctx context.Context)error{if s.DB==nil{return errors.New("delivery policy database required")};_,err:=s.DB.ExecContext(ctx,deliveryPolicySchema);return err}
func (s DeliveryPolicyStore)CheckAndReserve(ctx context.Context,request DeliveryRequest)(PolicyDecision,error){if s.DB==nil||s.ResolveLimit==nil{return PolicyDecision{},errors.New("delivery policy dependencies required")};if err:=validateDeliveryRequest(request);err!=nil{return PolicyDecision{},err};limit,err:=s.ResolveLimit(ctx,request.TenantID,request.DomainID,request.MailboxID);if err!=nil{return PolicyDecision{},err};tx,err:=s.DB.BeginTx(ctx,&sql.TxOptions{Isolation:sql.LevelSerializable});if err!=nil{return PolicyDecision{},err};defer tx.Rollback();var prior PolicyDecision;err=tx.QueryRowContext(ctx,`SELECT decision,decision_code,created_at FROM mail_delivery_reservations_v2 WHERE request_id=?`,request.ID).Scan(&prior.Decision,&prior.Code,&prior.ReservedAt);if err==nil{prior.RequestID=request.ID;if err=tx.Commit();err!=nil{return PolicyDecision{},err};return prior,nil};if !errors.Is(err,sql.ErrNoRows){return PolicyDecision{},err};hour:=request.At.UTC().Truncate(time.Hour).Format(time.RFC3339);month:=request.At.UTC().Format("2006-01");var hourMessages,hourRecipients,monthMessages,monthRecipients uint64;if err=tx.QueryRowContext(ctx,`SELECT COALESCE(SUM(messages),0),COALESCE(SUM(recipients),0) FROM mail_delivery_reservations_v2 WHERE tenant_id=? AND mailbox_id=? AND hour_bucket=? AND decision=?`,request.TenantID,request.MailboxID,hour,DeliveryPermit).Scan(&hourMessages,&hourRecipients);err!=nil{return PolicyDecision{},err};if err=tx.QueryRowContext(ctx,`SELECT COALESCE(SUM(messages),0),COALESCE(SUM(recipients),0) FROM mail_delivery_reservations_v2 WHERE tenant_id=? AND mailbox_id=? AND month_bucket=? AND decision=?`,request.TenantID,request.MailboxID,month,DeliveryPermit).Scan(&monthMessages,&monthRecipients);err!=nil{return PolicyDecision{},err};decision:=PolicyDecision{RequestID:request.ID,Decision:DeliveryPermit,Code:"permit",HourMessages:hourMessages+1,MonthMessages:monthMessages+1,ReservedAt:request.At.UTC()};recipientCount:=uint64(len(request.Recipients));switch{case !request.Authenticated:decision.Decision=DeliveryReject;decision.Code="authentication_required";decision.Detail="authenticated submission is required";case request.MessageBytes>limit.MaxMessageBytes:decision.Decision=DeliveryReject;decision.Code="message_too_large";decision.Detail="message exceeds policy";case len(request.Recipients)>int(limit.MaxRecipientsPerMessage):decision.Decision=DeliveryReject;decision.Code="recipient_limit";decision.Detail="too many recipients";case limit.HourlyMessages>0&&hourMessages+1>limit.HourlyMessages:decision.Decision=DeliveryDefer;decision.Code="hourly_message_limit";case limit.HourlyRecipients>0&&hourRecipients+recipientCount>limit.HourlyRecipients:decision.Decision=DeliveryDefer;decision.Code="hourly_recipient_limit";case limit.MonthlyMessages>0&&monthMessages+1>limit.MonthlyMessages:decision.Decision=DeliveryDefer;decision.Code="monthly_message_limit";case limit.MonthlyRecipients>0&&monthRecipients+recipientCount>limit.MonthlyRecipients:decision.Decision=DeliveryDefer;decision.Code="monthly_recipient_limit"};messages:=0;recipients:=0;bytesReserved:=uint64(0);if decision.Decision==DeliveryPermit{messages=1;recipients=len(request.Recipients);bytesReserved=request.MessageBytes};if _,err=tx.ExecContext(ctx,`INSERT INTO mail_delivery_reservations_v2(request_id,tenant_id,domain_id,mailbox_id,hour_bucket,month_bucket,messages,recipients,bytes,decision,decision_code,created_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?)`,request.ID,request.TenantID,request.DomainID,request.MailboxID,hour,month,messages,recipients,bytesReserved,decision.Decision,decision.Code,decision.ReservedAt);err!=nil{return PolicyDecision{},err};if err=tx.Commit();err!=nil{return PolicyDecision{},err};return decision,nil}
func validateDeliveryRequest(request DeliveryRequest)error{if !validOpaque(request.ID)||!validOpaque(request.TenantID)||request.DomainID==""||request.MailboxID==""||request.At.IsZero()||request.MessageBytes==0||len(request.Recipients)==0||len(request.Recipients)>10000{return ErrInvalidCommand};if _,err:=netip.ParseAddr(request.ClientIP);err!=nil{return ErrInvalidCommand};if err:=ValidateAddress(request.EnvelopeFrom);err!=nil{return err};if _,err:=NormalizeAddresses(request.Recipients);err!=nil{return err};return nil}

type PolicyProtocolServer struct { Store DeliveryPolicyStore; MaxLineBytes int }
// Decide parses a deliberately tiny attribute map from the Postfix policy
// protocol adapter. The network-facing adapter owns framing and never forwards
// arbitrary attribute names into SQL or an executor.
func (s PolicyProtocolServer)Decide(ctx context.Context,attributes map[string]string)(PolicyDecision,error){allowed:=map[string]bool{"request_id":true,"tenant_id":true,"domain_id":true,"mailbox_id":true,"sasl_username":true,"client_address":true,"sender":true,"recipient_count":true,"recipients":true,"size":true};for key,value:=range attributes{if !allowed[key]||len(key)+len(value)>s.lineLimit(){return PolicyDecision{},ErrInvalidCommand}};recipientsRaw:=strings.Split(attributes["recipients"],",");recipients:=make([]Address,0,len(recipientsRaw));for _,value:=range recipientsRaw{if value!=""{recipients=append(recipients,Address(value))}};size,err:=parseUint(attributes["size"]);if err!=nil{return PolicyDecision{},err};request:=DeliveryRequest{ID:attributes["request_id"],TenantID:attributes["tenant_id"],DomainID:DomainID(attributes["domain_id"]),MailboxID:MailboxID(attributes["mailbox_id"]),Authenticated:attributes["sasl_username"]!="",ClientIP:attributes["client_address"],EnvelopeFrom:Address(attributes["sender"]),Recipients:recipients,MessageBytes:size,At:time.Now().UTC()};return s.Store.CheckAndReserve(ctx,request)}
func (s PolicyProtocolServer)lineLimit()int{if s.MaxLineBytes<=0{return 4096};return s.MaxLineBytes}
func parseUint(value string)(uint64,error){var result uint64;if value==""{return 0,ErrInvalidCommand};for _,r:=range value{if r<'0'||r>'9'{return 0,ErrInvalidCommand};next:=result*10+uint64(r-'0');if next<result{return 0,fmt.Errorf("numeric overflow")};result=next};return result,nil}
